//! Generated gRPC code for the node service.

/// Generated gRPC types for the `quark.node.v1` package.
///
/// These types are produced by `tonic_build` from `proto/node.proto`. They
/// intentionally do not carry per-item Rust doc comments (prost does not
/// translate `.proto` comments to Rust doc attributes), so we suppress the
/// workspace `missing_docs` lint inside this module to keep the build clean.
#[allow(missing_docs)]
pub mod v1 {
    tonic::include_proto!("quark.node.v1");
}
